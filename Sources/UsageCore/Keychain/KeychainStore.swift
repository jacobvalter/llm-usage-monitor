import Foundation

/// Stores and retrieves provider admin API keys from the macOS Keychain.
/// Keys never leave the local machine (not synced via CloudKit).
public struct KeychainStore: Sendable {
    // TODO: Implement in step 6
}
