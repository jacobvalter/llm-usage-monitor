import Foundation

/// Orchestrates periodic polling of all configured provider clients
/// and writes normalized UsageEvents into the local store.
public actor PollScheduler {
    // TODO: Implement in step 5
}
